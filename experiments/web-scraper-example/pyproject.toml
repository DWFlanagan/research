[project]
name = "web-scraper-example"
version = "0.1.0"
description = "Example web scraper using httpx and BeautifulSoup"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
]
